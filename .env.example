# Public — safe in the browser bundle
VITE_PAY_BASE_URL=https://api-pay.agent.tech

# Default creator/agent EVM wallet that receives tips (0x…)
VITE_CREATOR_RECIPIENT=

# Do NOT put PAY_API_KEY or PAY_SECRET_KEY here — they would ship to the browser.
# Server-side PayClient keys belong only in a separate backend, not this Vite app.
